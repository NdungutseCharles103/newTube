export interface IPlayerState {
    isPlaying: boolean,
    progress: number,
    speed: number,
    isMuted: boolean,
    isFullScreen: boolean,
    isPicInPic: boolean,
    volume: number,
    loop: string
    shuffle: boolean;
}